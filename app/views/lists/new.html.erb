<div class="container">
  <h1>New list</h1>
  <%= simple_form_for [@list] do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Submit new list", class: "btn btn-primary" %>
  <% end %>
</div>
